;; Copy this file to local-config.edn (or local-config.edn.gpg for encryption)
;; and fill in your values.
;;
;; Servers listed here get their :config written to config.edn in each tool dir.
;; Servers not listed use defaults from their existing config.edn.
;; Set :enabled false to exclude a server entirely.

{;; ECA/Claude Code settings (merged into config.json top-level)
 :eca
 {:defaultModel "anthropic/claude-sonnet-4-20250514"
  :agent {:code {:variant "max"}
          :plan {:variant "max"}}}

 :servers
 {:slack {:config {:default-host "your-workspace.slack.com"}}
  :splunk {:config {:host "your-instance.splunkcloud.com"}}
  :org-roam-mcp {:config {:ollama-url "http://localhost:11434"
                           :ssh-tunnel {:enabled false
                                        :host "your-ollama-host"
                                        :remote-port 11434
                                        :local-port 11434}}}}

 ;; Extra content appended to AGENTS.md (optional)
 ;; :agents-extra "..."
}
